{# empty Twig template #}
<!DOCTYPE html>
<html>
    <head>
        <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
        <title>Projektor | Grafia.cz | Přihlášení k systému</title>
        <link rel="icon" type="image/gif" href="favicon.gif" />
        <link rel="stylesheet" type="text/css" href="css/default.css" />
        <link rel="stylesheet" type="text/css" href="css/highlight.css" />
        <link rel="stylesheet" type="text/css" href="css/quickform.css" />
        {{ js_libraries|raw }}  {# javascript pro quickform je předáván v proměnné js_libraries #}
    </head>
    <body>
        {% if login_warning %}
            <span  style="color: red"><strong>{{ login_warning }}</strong></span>
        {% endif %}
        {% import 'macro_quickform_twig.twig' as quickform %}
        {% block form %}
            {{ quickform.quickform(form) }}
        {% endblock form %}
    </body>
</html>